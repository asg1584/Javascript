/*Return the provided string with the first letter of each word capitalized. Make sure the rest of the word is in lower case.

For the purpose of this exercise, you should also capitalize connecting words like the and of.*/

function titleCase(str) {
  const newTitle = str.split(" ");//The input string str is split by space characters using split(" ").
  const updatedTitle = [];
  for (let st in newTitle) { //Next, a loop (for...in) is used to iterate through the newTitle array.
    updatedTitle[st] = newTitle[st][0].toUpperCase() + newTitle[st].slice(1).toLowerCase();
  }
  return updatedTitle.join(" "); //Once the loop is completed, you have all the words with their first letters capitalized and the rest in lowercase stored in the updatedTitle array.
}


//So, in summary, the st variable is used within the loop to represent the index, and it determines the position of each word in the array.
